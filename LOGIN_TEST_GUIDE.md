# 登录功能测试指南

本指南将帮助您测试Vue项目的登录功能，确保使用Mocker Server登录后能正确跳转到首页。

## 已完成的修改

1. 修复了LoginView.vue中的跳转逻辑错误：
   - 原代码：`router.push(redirect || '/')`
   - 修改为：`router.push(redirect.value || '/')`

## 测试步骤

### 1. 启动开发服务器

在项目根目录下运行以下命令启动开发服务器：

```bash
npm run dev
```

或者如果您使用yarn：

```bash
yarn dev
```

### 2. 访问登录页面

打开浏览器，访问开发服务器地址（通常是 http://localhost:5173/login）

### 3. 使用默认账户登录

系统提供了两个默认账户用于测试：

1. 管理员账户：
   - 用户名：admin
   - 密码：123456

2. 普通用户账户：
   - 用户名：user
   - 密码：123456

### 4. 验证登录结果

登录成功后，系统应该自动跳转到首页（/）。

您可以通过以下方式验证登录是否成功：
1. 观察页面是否跳转到了首页
2. 检查页面右上角是否显示了用户头像下拉菜单（表示已登录状态）
3. 查看浏览器控制台是否有错误信息

### 5. 登录失败测试

您可以尝试输入错误的用户名或密码来测试登录失败的情况：
1. 输入错误的用户名或密码
2. 点击登录按钮
3. 系统应提示"用户名或密码错误"
4. 此时应显示验证码输入框

## 技术说明

登录功能基于LocalStorage实现，用户数据存储在浏览器的LocalStorage中。

### 默认用户数据

系统初始化时会自动创建两个默认用户：
- admin/123456 （管理员）
- user/123456 （普通用户）

### 存储键值

- `mock_users`: 存储所有用户信息
- `mock_current_user`: 存储当前登录用户的用户名
- `saved_username`: 存储记住的用户名（如果勾选了"记住我"选项）

## 故障排除

如果登录后没有正确跳转，请检查：

1. 浏览器控制台是否有JavaScript错误
2. 网络请求是否正常（虽然这是Mock服务，但仍需检查）
3. LocalStorage中是否正确保存了用户信息

您可以通过浏览器开发者工具的Application/Storage标签页查看LocalStorage内容。